{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const ua=(process.env.npm_config_user_agent||''); if(!ua.startsWith('pnpm/')){console.error('Use pnpm instead'); process.exit(1);}\"",
    "dev": "corepack pnpm -r --parallel --filter @workspace/api-server --filter @workspace/task-manager run dev",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "deploy:build": "corepack pnpm --filter @workspace/task-manager run build",
    "deploy:check": "corepack pnpm --filter @workspace/task-manager run typecheck && corepack pnpm --filter @workspace/api-server run typecheck && corepack pnpm run deploy:build",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
